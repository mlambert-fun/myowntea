Local PostgreSQL for this project lives at:

`%LOCALAPPDATA%\MyOwnTea\PostgreSQL\18\data`

Use that cluster for local development and for `dev: full stack`.

Do not use `backend/.postgres` as the active database path. That repo-local path is kept only as an old broken artifact and may be a OneDrive placeholder.
